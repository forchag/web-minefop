# Re-exported from apps.core.validators (shared with other apps) — kept here
# too because historical migrations reference this import path directly.
from apps.core.validators import validate_file_size  # noqa: F401
